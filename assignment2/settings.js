mx.settings.sound.background = "forrest.mp3";         // play forrest.mp3 in the background
mx.settings.component.list.push("graph");             // include the Graph component
mx.settings.character.list.push("alien", "bullet");   // load the Alien and Bullet character maps

mx.settings.movement.clicks = false;                  // turn off click character selection
mx.settings.movement.shortcuts = true;                // turn on arrow key character movement

mx.settings.placement.animate = false;                // character movement is animated
mx.settings.gravity.build_wall = true;                // build a wall around the viewport

mx.settings.dom.center = true;                        // center the view port on the screen
mx.settings.dom.width = 200;                          // set the viewport's width
mx.settings.dom.height = 200;                         // set the viewport's height
mx.settings.dom.dtype = mx.dom.type._2D;              // set the display type to 2D mode
